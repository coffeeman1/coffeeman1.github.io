import{_ as o,o as c,c as t,a2 as d}from"./chunks/framework.DvFsUg-r.js";const m=JSON.parse('{"title":"Hexo 블로그 콘텐츠 임포트 자동화 — Tasks","description":"","frontmatter":{"title":"Hexo 블로그 콘텐츠 임포트 자동화 — Tasks","feature_id":"008","created":"2026-06-02 15:30 (KST)","date":"2026-06-02T00:00:00.000Z"},"headers":[],"relativePath":"specs/008-hexo-blog-import/tasks.md","filePath":"specs/008-hexo-blog-import/tasks.md","lastUpdated":null}'),s={name:"specs/008-hexo-blog-import/tasks.md"};function a(r,e,i,l,n,p){return c(),t("div",null,[...e[0]||(e[0]=[d(`<h1 id="tasks-hexo-블로그-콘텐츠-임포트-자동화" tabindex="-1">Tasks: Hexo 블로그 콘텐츠 임포트 자동화 <a class="header-anchor" href="#tasks-hexo-블로그-콘텐츠-임포트-자동화" aria-label="Permalink to &quot;Tasks: Hexo 블로그 콘텐츠 임포트 자동화&quot;">​</a></h1><p><strong>입력 설계 문서</strong>: <a href="./plan">plan.md</a> · <a href="./spec">spec.md</a> · <a href="./research">research.md</a> · <a href="./data-model">data-model.md</a> · <a href="./contracts/importer-cli">contracts/importer-cli.md</a> · <a href="./contracts/transform-rules">contracts/transform-rules.md</a> · <a href="./quickstart">quickstart.md</a></p><p><strong>구현 산출물</strong>: <code>scripts/import-hexo-blog.mjs</code> (Node ESM, 의존 0) → <code>blog/blog/blog-archive/**</code>, <code>blog/blog/.vitepress/blog-archive-sidebar.mts</code>, <code>blog/blog/.vitepress/config.mts</code>(마커 주입).</p><blockquote><p>경로 규약: 리포 루트 <code>C:/ax/blog</code>. 원본 <code>../coffeeman1-main</code> 은 읽기 전용. 검증 게이트: <code>cd blog &amp;&amp; npm run build</code>(VitePress strict).</p></blockquote><hr><h2 id="phase-1-setup-프로젝트-초기화" tabindex="-1">Phase 1: Setup (프로젝트 초기화) <a class="header-anchor" href="#phase-1-setup-프로젝트-초기화" aria-label="Permalink to &quot;Phase 1: Setup (프로젝트 초기화)&quot;">​</a></h2><ul><li>[X] T001 <code>scripts/import-hexo-blog.mjs</code> 빈 ESM 진입점 생성 — shebang(<code>#!/usr/bin/env node</code>), <code>main()</code> 골격, <code>import { argv } from &#39;node:process&#39;</code>, 최상단 상수 영역(SRC 기본값 <code>../coffeeman1-main</code>, OUT <code>blog/blog/blog-archive</code>) 배치</li><li>[X] T002 [P] <code>scripts/import-hexo-blog.test.mjs</code> 생성 — <code>node --test</code>(Node 내장) 기반 빈 테스트 파일 + 픽스처 디렉터리 <code>scripts/__fixtures__/</code> 스캐폴드</li><li>[X] T003 [P] 변환 상수 모듈화 — <code>mjs</code> 상단에 카테고리 영문 라벨 매핑표·FA→이모지 매핑표·callout 타입 매핑을 <a href="./contracts/transform-rules">contracts/transform-rules.md</a> 표대로 상수 선언</li></ul><p><strong>체크포인트</strong>: <code>node scripts/import-hexo-blog.mjs --dry-run</code> 이 에러 없이 종료(아직 아무 것도 안 함).</p><hr><h2 id="phase-2-foundational-모든-스토리의-선행-차단-작업" tabindex="-1">Phase 2: Foundational (모든 스토리의 선행 차단 작업) <a class="header-anchor" href="#phase-2-foundational-모든-스토리의-선행-차단-작업" aria-label="Permalink to &quot;Phase 2: Foundational (모든 스토리의 선행 차단 작업)&quot;">​</a></h2><p><strong>목적</strong>: 모든 스토리가 의존하는 공용 입출력·파싱 유틸. 완료 전 US1~US3 착수 불가.</p><ul><li>[X] T004 <code>parseArgs(argv)</code> 구현 in <code>scripts/import-hexo-blog.mjs</code> — <code>--source</code>/<code>--dry-run</code>/<code>--quiet</code> 파싱, 종료코드 규약(<a href="./contracts/importer-cli">contracts/importer-cli.md</a>)</li><li>[X] T005 사전조건 검사 — <code>--source</code> 루트와 <code>source/_posts/</code> 부재 시 코드 <code>1</code> 로 즉시 종료(아무 것도 쓰지 않음, FR-11)</li><li>[X] T006 <code>parseFrontmatter(raw)</code> 구현 — 최소 YAML 파서(title/date/categories/tags) + 본문 분리. <code>SourcePost</code> 형상(<a href="./data-model">data-model.md</a>)으로 반환</li><li>[X] T007 <code>discoverPosts(srcRoot)</code> 구현 — <code>source/_posts/**/*.md</code> 수집(<code>.png</code>/바이너리/<code>img.png</code> 류 제외), 폴더 경로 → 카테고리 추론, <code>assetDir</code> 탐지</li><li>[X] T008 [P] 안전 파일 IO 헬퍼 — <code>rmrf(dir)</code>(전량 삭제)·<code>writeFileUtf8NoBom(path, text)</code>(BOM 없는 UTF-8)·<code>copyAsset(src, dst)</code> in <code>scripts/import-hexo-blog.mjs</code></li><li>[X] T009 [P] [Foundational 검증] <code>parseFrontmatter</code>·<code>discoverPosts</code> 단위 테스트 in <code>scripts/import-hexo-blog.test.mjs</code> — 픽스처로 제목/날짜/카테고리 파싱·글 수 검증</li></ul><p><strong>체크포인트</strong>: 원본을 읽어 <code>SourcePost[]</code> 를 메모리에 구성할 수 있다(쓰기 전 단계).</p><hr><h2 id="phase-3-user-story-1-—-단일-명령-전체-임포트-이미지-동작-strict-빌드-통과-p1-🎯-mvp" tabindex="-1">Phase 3: User Story 1 — 단일 명령 전체 임포트(이미지 동작 + strict 빌드 통과) (P1) 🎯 MVP <a class="header-anchor" href="#phase-3-user-story-1-—-단일-명령-전체-임포트-이미지-동작-strict-빌드-통과-p1-🎯-mvp" aria-label="Permalink to &quot;Phase 3: User Story 1 — 단일 명령 전체 임포트(이미지 동작 + strict 빌드 통과) (P1) 🎯 MVP&quot;">​</a></h2><p><strong>스토리 목표</strong>: 관리자가 명령 한 번으로 모든 Hexo 글을 VitePress <code>/blog-archive/</code> 섹션으로 옮기고, 이미지가 표시되며, <code>npm run build</code>(strict)가 통과한다. (스펙 Scenario 1·3 / FR-1·2·3·4·5·6·7·8 / SC-1·2·3·5·7)</p><p><strong>독립 테스트 기준</strong>: <code>node scripts/import-hexo-blog.mjs</code> 실행 → <code>cd blog &amp;&amp; npm run build</code> 성공 → dev 서버에서 &quot;Blog Archive&quot; 내비·사이드바·표본 글 이미지 확인.</p><ul><li>[X] T010 [US1] <code>mapCategory(cats, folderPath)</code> 구현 in <code>scripts/import-hexo-blog.mjs</code> — R6 매핑표로 <code>categoryKey</code>+영문 <code>label</code>, 빈 값 <code>Uncategorized</code></li><li>[X] T011 [US1] <code>resolveImages(body, post)</code> 구현 — 표준 마크다운 이미지(<code>![](../../images/x)</code>)와 <code>{% asset_img %}</code> 를 글 asset→전역 <code>images/</code>·<code>img/</code>→상대경로 순으로 해석, <code>&lt;slug&gt;.assets/</code> 로 복사 후 <code>![](./&lt;slug&gt;.assets/x)</code> 재작성(<a href="./contracts/transform-rules">contracts/transform-rules.md</a> 이미지 절·data-model <code>ImageRef</code>)</li><li>[X] T012 [US1] 해석 실패 이미지 처리 — 링크 원형 유지 + <code>imagesMissing</code> 집계(빌드 비차단, FR-5)</li><li>[X] T013 [US1] <code>sanitizeVue(body)</code> 구현 — 코드펜스/인라인코드 밖의 <code>{{ }}</code>→<code>&lt;span v-pre&gt;…&lt;/span&gt;</code>, 비표준 <code>&lt;tag&gt;</code> 이스케이프(R5, FR-7), 정상 HTML/<code>&lt;style&gt;</code>/<code>&lt;table&gt;</code> 보존</li><li>[X] T014 [US1] 비이미지 Hexo 태그 <strong>안전 처리(스텁)</strong> — <code>{% fa %}</code>/<code>{% callout %}</code>/<code>{% endcallout %}</code>/<code>{% iframe %}</code>/<code>{% drawio %}</code>/<code>{% incl %}</code>/<code>{% raw %}</code> 을 빌드 깨지 않게 임시 제거(다음 스토리에서 등가 변환으로 대체). 출력에 잔여 <code>{% %}</code> 0 보장</li><li>[X] T015 [US1] <code>emitFrontmatter(post)</code> 구현 — <code>title</code>/<code>date</code>(ISO+09:00)/<code>tags</code>/<code>editLink:false</code>/<code>lastUpdated:false</code> 직렬화(<a href="./contracts/transform-rules">contracts/transform-rules.md</a> 프런트매터 절)</li><li>[X] T016 [US1] <code>writeSection(posts)</code> 구현 — <code>blog-archive/**</code> 전량 재생성, 글을 <code>blog-archive/&lt;categoryKey&gt;/&lt;slug&gt;.md</code> 로 기록(<code>--dry-run</code> 시 미기록)</li><li>[X] T017 [US1] <code>writeIndex(groups)</code> 구현 — <code>blog-archive/index.md</code>(hero 없는 카테고리 개요 + 각 그룹 최신글, <code>date</code> 내림차순, FR-3·R2)</li><li>[X] T018 [US1] <code>writeSidebar(groups)</code> 구현 — <code>blog/blog/.vitepress/blog-archive-sidebar.mts</code> 에 <code>export const blogArchiveSidebar = { &#39;/blog-archive/&#39;: [...] }</code>, 그룹 라벨 영문·항목 최신 먼저</li><li>[X] T019 [US1] <code>wireConfig()</code> 구현 — <code>blog/blog/.vitepress/config.mts</code> 에 마커 블록(import·nav <code>{ text: &#39;Blog Archive&#39;, link: &#39;/blog-archive/&#39; }</code>·<code>...blogArchiveSidebar</code>) 멱등 주입, 마커 부재 시 <code>...hermesSidebar,</code> 앵커 뒤 최초 1회 삽입(R7)</li><li>[X] T020 [US1] <code>main()</code> 파이프라인 연결 — discover→parseFrontmatter→mapCategory→(태그 안전처리 T014)→resolveImages→sanitizeVue→writeSection→writeIndex→writeSidebar→wireConfig</li><li>[X] T021 [US1] 실제 임포트 실행 후 <code>cd blog &amp;&amp; npm run build</code> 통과 확인(SC-2). 실패 시 로그의 파일·라인으로 T011/T013/T014 보강</li><li>[X] T022 [P] [US1] 표본 글 3편 수동 검수 — 달력 글(인라인 <code>&lt;style&gt;</code>/table), jhipster(asset_img 다수), setup(전역 images 상대경로) 이미지 표시 100% 확인(SC-3)</li></ul><p><strong>체크포인트</strong>: ✅ MVP — 모든 글이 사이트에 표시되고 빌드가 통과한다(특수 문법은 미표현이나 빌드 무해).</p><hr><h2 id="phase-4-user-story-2-—-hexo-전용-문법-충실-변환-p2" tabindex="-1">Phase 4: User Story 2 — Hexo 전용 문법 충실 변환 (P2) <a class="header-anchor" href="#phase-4-user-story-2-—-hexo-전용-문법-충실-변환-p2" aria-label="Permalink to &quot;Phase 4: User Story 2 — Hexo 전용 문법 충실 변환 (P2)&quot;">​</a></h2><p><strong>스토리 목표</strong>: US1 에서 안전 제거했던 비이미지 Hexo 태그를 VitePress 등가 표현으로 승격해 시각적 의도를 보존하고, 출력에 잔여 <code>{% %}</code> 0 을 보장한다. (스펙 FR-13·14·15 / SC-8 / Clarifications)</p><p><strong>독립 테스트 기준</strong>: 특수 문법 글(<code>blog-fontawesome</code>·<code>blog-hexo</code>·<code>blog-hexo-include</code>)에서 아이콘·callout·included 본문이 표현되고, <code>grep -r &quot;{%&quot; blog/blog/blog-archive</code> 결과 0건.</p><ul><li>[X] T023 [US2] <code>convertTags</code> 로 T014 스텁 대체 — <code>{% asset_img %}</code> 는 T011 과 연동 유지, 나머지 태그를 등가 변환 디스패처로 재구성 in <code>scripts/import-hexo-blog.mjs</code></li><li>[X] T024 [P] [US2] <code>{% fa icon [size] %}</code>·<code>{% fa_all_free_class %}</code> → 이모지 매핑(미매핑 제거), size 무시(<a href="./contracts/transform-rules">contracts/transform-rules.md</a> FA 표)</li><li>[X] T025 [P] [US2] <code>{% callout type:&quot;X&quot; title:&quot;T&quot; %}…{% endcallout %}</code> → VitePress 컨테이너(<code>::: tip/warning/danger</code> + title), info/success→tip 매핑</li><li>[X] T026 [P] [US2] <code>{% iframe SRC W H %}</code> → <code>&lt;iframe … loading=&quot;lazy&quot;&gt;</code>, <code>{% drawio f %}</code> → 렌더 이미지 있으면 이미지·없으면 자산 다운로드 링크</li><li>[X] T027 [US2] <code>expandIncludes(body, srcRoot, depth)</code> 구현 — <code>{% incl name %}</code> → <code>source/_includes/name.md</code> 재귀 인라인(깊이≤8·순환 가드), 미발견 시 제거+<code>includesMissing</code> 집계(FR-14, R-5). 파이프라인에서 convertTags <strong>이전</strong>에 실행</li><li>[X] T028 [US2] <code>{% raw %}…{% endraw %}</code> 언랩 + 잔여 미지 <code>{% … %}</code> 안전 제거 후 <code>tagsUnknown</code> 집계(FR-15)</li><li>[X] T029 [US2] 재임포트 후 <code>cd blog &amp;&amp; npm run build</code> 재통과 + <code>grep -rn &quot;{%&quot; blog/blog/blog-archive</code> 0건 확인(SC-8)</li><li>[X] T030 [P] [US2] <code>convertTags</code>·<code>expandIncludes</code> 단위 테스트 in <code>scripts/import-hexo-blog.test.mjs</code> — fa(매핑/미매핑)·callout 4종·iframe·drawio(유/무)·incl(재귀/미발견)·raw 입출력 쌍</li></ul><p><strong>체크포인트</strong>: ✅ 특수 문법이 등가 표현으로 보존되고 잔여 태그 0.</p><hr><h2 id="phase-5-user-story-3-—-멱등-재실행-임포트-리포트-p2" tabindex="-1">Phase 5: User Story 3 — 멱등 재실행 + 임포트 리포트 (P2) <a class="header-anchor" href="#phase-5-user-story-3-—-멱등-재실행-임포트-리포트-p2" aria-label="Permalink to &quot;Phase 5: User Story 3 — 멱등 재실행 + 임포트 리포트 (P2)&quot;">​</a></h2><p><strong>스토리 목표</strong>: 원본에 글이 추가된 뒤 재실행해도 중복·고아 없이 동일 결과를 내고, 무엇이 옮겨졌고 무엇이 실패했는지 요약을 제공한다. (스펙 Scenario 2·4 / FR-9·10 / SC-4·6, 종료코드 규약)</p><p><strong>독립 테스트 기준</strong>: 연속 2회 실행 후 <code>git status</code>/디렉터리 diff 가 빈 결과(타임스탬프 제외), 콘솔 요약·<code>.import-report.json</code> 에 정확한 집계 표시.</p><ul><li>[X] T031 [US3] <code>ImportReport</code> 누산기 구현 — postsConverted/imagesCopied/imagesMissing/includesExpanded/includesMissing/tagsUnknown/skipped 집계(<a href="./data-model">data-model.md</a>)</li><li>[X] T032 [US3] 콘솔 요약 출력 — <a href="./contracts/importer-cli">contracts/importer-cli.md</a> 형식, <code>--quiet</code> 시 요약만</li><li>[X] T033 [US3] <code>blog/blog/blog-archive/.import-report.json</code> 기록 + <code>.gitignore</code> 에 해당 파일 추가</li><li>[X] T034 [US3] 종료코드 적용 — 정상 <code>0</code>, 사전조건 실패 <code>1</code>, 잔여 미지 태그&gt;0 또는 누락 존재 시 <code>2</code>(CI 게이트는 <code>0</code> 만 통과)</li><li>[X] T035 [US3] 멱등성 보강 — <code>blog-archive/**</code>·사이드바 전량 재생성 결정성 확보(정렬·키 순서 고정, 타임스탬프 외 바이트 동일)</li><li>[X] T036 [P] [US3] 멱등 통합 테스트 — 임포트 2회 실행 후 산출물 동일(diff 빈 결과) 확인 스크립트/절차 in <code>scripts/import-hexo-blog.test.mjs</code>(SC-4)</li></ul><p><strong>체크포인트</strong>: ✅ 재동기화 안전 + 운영 가시성 확보.</p><hr><h2 id="phase-6-polish-cross-cutting" tabindex="-1">Phase 6: Polish &amp; Cross-Cutting <a class="header-anchor" href="#phase-6-polish-cross-cutting" aria-label="Permalink to &quot;Phase 6: Polish &amp; Cross-Cutting&quot;">​</a></h2><ul><li>[X] T037 [P] 얇은 진입 래퍼 추가(선택) — <code>scripts/import-hexo-blog.ps1</code>·<code>scripts/import-hexo-blog.sh</code> 가 <code>node scripts/import-hexo-blog.mjs @args</code> 호출(FR-12, 선례 명령 흐름 일관)</li><li>[X] T038 [P] 저널 작성 — <code>blog/blog/journal/2026-06-02-hexo-blog-import.md</code>(CLAUDE.md <code>created</code>/<code>date</code> 규칙 준수, 최신 먼저)</li><li>[X] T039 [P] 임포터 사용법을 <code>blog/CLAUDE.md</code> 또는 <code>README</code> 의 도구 목록에 한 줄 추가(<code>sync-*</code> 옆)</li><li>[X] T040 전체 검증 — <a href="./quickstart">quickstart.md</a> 수용 체크리스트 SC-1~SC-8 전부 통과 확인</li><li>[X] T041 spec status <code>draft</code>→<code>done</code> 갱신 후 <code>cd blog &amp;&amp; npm run build</code> 최종 통과 + 게시 확인</li></ul><hr><h2 id="dependencies-실행-순서" tabindex="-1">Dependencies &amp; 실행 순서 <a class="header-anchor" href="#dependencies-실행-순서" aria-label="Permalink to &quot;Dependencies &amp; 실행 순서&quot;">​</a></h2><ul><li><strong>Phase 1 (Setup)</strong> → <strong>Phase 2 (Foundational)</strong> 가 모든 스토리를 차단.</li><li><strong>US1(P1)</strong> 은 Foundational 이후 착수 — <strong>MVP</strong>. 단독으로 배포 가능(특수 문법 미표현).</li><li><strong>US2(P2)</strong> 는 US1 의 <code>convertTags</code> 스텁(T014)·파이프라인(T020)에 의존 → US1 이후.</li><li><strong>US3(P2)</strong> 는 US1 의 쓰기 경로·파이프라인에 의존. US2 와는 독립(병행 가능)이나 <code>tagsUnknown</code> 집계(T031/T034)는 US2 의 분류와 맞물리므로 US2 후 권장.</li><li><strong>Polish</strong> 는 마지막.</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Setup(T001-003) → Foundational(T004-009)</span></span>
<span class="line"><span>        → US1(T010-022) ──┬─→ US2(T023-030)</span></span>
<span class="line"><span>                          └─→ US3(T031-036)   ← US2/US3 일부 병행 가능</span></span>
<span class="line"><span>                                   → Polish(T037-041)</span></span></code></pre></div><h2 id="병렬-실행-기회-p-표시" tabindex="-1">병렬 실행 기회 ([P] 표시) <a class="header-anchor" href="#병렬-실행-기회-p-표시" aria-label="Permalink to &quot;병렬 실행 기회 ([P] 표시)&quot;">​</a></h2><ul><li><strong>Setup</strong>: T002, T003 동시(서로 다른 파일/상수).</li><li><strong>Foundational</strong>: T008, T009 동시(IO 헬퍼 vs 테스트).</li><li><strong>US1</strong>: 구현 직렬(같은 파일 <code>mjs</code>)이나 검수 T022 는 빌드 후 병렬 가능.</li><li><strong>US2</strong>: T024·T025·T026 은 서로 다른 태그 변환 함수라 분담 가능(디스패처 T023 합류 전 각자 작성).</li><li><strong>US3</strong>: T036(통합 테스트)은 T031~T035 구현과 병렬 준비 가능.</li><li><strong>Polish</strong>: T037·T038·T039 동시(서로 다른 파일).</li></ul><blockquote><p>주의: 핵심 변환 로직은 단일 파일 <code>scripts/import-hexo-blog.mjs</code> 에 모이므로 같은 파일을 만지는 [P] 미표시 태스크는 순차 진행한다.</p></blockquote><h2 id="implementation-strategy" tabindex="-1">Implementation Strategy <a class="header-anchor" href="#implementation-strategy" aria-label="Permalink to &quot;Implementation Strategy&quot;">​</a></h2><ul><li><strong>MVP 먼저</strong>: Phase 1·2 + <strong>US1</strong> 만으로 &quot;전 글 임포트 + 이미지 + strict 빌드 통과&quot; 가 성립 → 바로 게시 가능한 최소 제품.</li><li><strong>점진 강화</strong>: US2(문법 충실) → US3(멱등·리포트) 순으로 가치 추가. 각 스토리는 독립 테스트 기준을 만족하면 개별 머지 가능.</li><li><strong>검증 게이트</strong>: 매 스토리 종료 시 <code>cd blog &amp;&amp; npm run build</code> 를 돌려 strict 통과를 확인(깨진 링크·Vue 충돌 0).</li></ul>`,43)])])}const h=o(s,[["render",a]]);export{m as __pageData,h as default};
